name: json5_plus
description: A high performance JSON5 parser with type-safe accessors, optional case-insensitive keys, and an "include" capability.
version: 0.0.5
repository: https://github.com/AndyKing42/json5_plus

environment:
  sdk: '>=3.11.1 <4.0.0'

dependencies:
  ffi: ^2.2.0
  meta: ^1.18.2
  win32: ^6.2.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
